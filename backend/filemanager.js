/* Filemanager*/
var Filemanager = function(){
   init: function(path) {
   }

	/*return {		
		addRule : function(name, rule) {

			rules[name] = rule;
		},
		getRule : function(name) {

			return rules[name];
		}
	}*/
}